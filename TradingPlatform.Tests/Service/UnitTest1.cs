using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using TradingPlatform.Models;
using TradingPlatform.Service;
using TradingPlatform.Service.Impl;

namespace TradingPlatform.Tests.Service
{
    [TestClass]
    public class ExecutionTest
    {
        [TestMethod]
        public void TestAddExecution()
        {
            //OrderBook orderbBook = new OrderBook { Symbol = "hello", IsBit = true, Orders = null, Quantity = 123, Price = 23.3, Traders = null }
            //Order order = new Order { DateTime = DateTime.Now, Execution = null, IsBuy = true, OrderBook = orderBook, Price = 12, Quantity = 12, Strategy = strategy, Trader = trader };
            //Execution execution = new Execution { DateTime = DateTime.Now, Order = null, Trades = null};

            //ExecutionService executionService = new ExecutionService();
            //executionService.AddExecution(execution);
        }
    }
}
